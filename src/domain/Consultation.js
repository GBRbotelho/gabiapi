class Consultation {
  constructor(client, service, date, time, session) {
    this.client = client;
    this.service = service;
    this.date = date;
    this.time = time;
  }
}

module.exports = Consultation;
